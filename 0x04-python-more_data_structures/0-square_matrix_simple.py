#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    martix_tmp = []
    for iter in range(led(matrix)):
        new = []
        for iter2 in range(len(matrix[0])):
            new.append(matrix[iter] [iter2] ** 2)
            matrix_tmp.append(new)
            return matrix_tmp
